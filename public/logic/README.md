# Logic layer (do not replace for UI)

Runtime game code and Supabase client config.

| File | Role |
|------|------|
| `category1.js` | Anon Wheel rules, sync, wheel, finals |
| `category2.js` | Mirror Vote rules, votes, results |
| `supabase-config.js` | Supabase URL + publishable key |

UI markup/CSS lives in `/public/ui/`. See `/public/ui/README.md`.
